<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <%= link_to "Newspapers", newspapers_path, class: "nav-link #{active_class("newspapers", "index")}"  %> 
        </li>
        <li class="nav-item active">
          <a><%= link_to "Home", root_path, class: "nav-link #{active_class("welcome", "index")}" %> </a>
        </li>
      </ul>

      <ul class="navbar-nav ml-auto">
        <% if admin_only %>
          <li class="nav-item">
            <a><%= link_to "Admin", rails_admin_path, class: "nav-link " %> </a>
          </li>
          
        <% end %>
        
        <% if user_signed_in? %>
          <li class="nav-item">
            <% if @newspaper.present? %>
              <%= link_to current_user.full_name, users_path(filter: current_user), class: "nav-link" %> 
            <% end %>
          </li>
           <li class="nav-item">
            <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link" %> 
          </li>
        <%else %>
            <li><%= link_to "Sign In", user_session_path, class: "nav-link" %></li>
            <li><%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %></li>
          </div>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
